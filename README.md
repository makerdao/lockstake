# LockstakeEngine

## Admin Functions

`rely(address usr)`

`deny(address usr)`

`file(bytes32 what, address data)`

`addFarm(address farm)`

`delFarm(address farm)`

## User Functions

`open() returns (address urn)`